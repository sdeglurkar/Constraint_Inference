Type matlabroot in the MATLAB command prompt and copy the path.
In terminal run 

cd "matlabroot/extern/engines/python"
python setup.py install

From the python file run

import matlab.engine
eng = matlab.engine.start_matlab()      <---- This takes a few seconds
eng.function(args)